This subfolder contains:

1: [training)site] List all the wildfire events need be in training dataset
2: [conf_sites.yaml] Contain properties of wildfire events required to process such as resampling re-projection and interpolation
